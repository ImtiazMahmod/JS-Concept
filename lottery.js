/* const random = Math.random()*75;
const rounded = Math.round(random);
console.log(rounded); */

for(let i= 0; i<14; i++ ){

    const random = Math.random()*75;
    const rounded = Math.round(random);
    console.log(rounded);
}